# By default FARO tries to OCR images but this can significantly slow the processing, to disable uncomment the next line
# FARO_DISABLE_OCR="YES"
# Number of seconds before FARO will timeout if the tika server does not respond (default: 60)
FARO_REQUESTS_TIMEOUT=150
# Bytes per character used in PDF mixed documents (text and images) to force OCRing the document (default: 150 bytes/char)
FARO_PDF_OCR_RATIO=200
# Faro Logging level (default: INFO)
FARO_LOG_LEVEL=INFO
# Faro Logging file (default: None). Inside output folder to ensure persistence in host machine
FARO_LOG_FILE=output/faro.log
